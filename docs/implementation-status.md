# Implementation Status

Living progress tracker. Update as work lands; do not let this drift from reality.

**Milestone 1: COMPLETE.** All quality gates pass (lint/typecheck/test/build), the full vertical slice is verified end-to-end against a live Postgres/Redis (real backtest via HTTP, deterministic rerun confirmed, QUEUED->COMPLETED transition observed live in the dashboard), and independent architecture/research-methodology/quality reviews all returned GO with zero BLOCKER/HIGH findings.

**Milestone 2: COMPLETE.** All quality gates pass, the full journal pipeline is verified end-to-end against live Postgres/Redis, and independent architecture/journal-completeness/research-methodology/quality reviews are all resolved — one HIGH finding (normalized-trade merge ordering, corrupting drawdown/streak metrics) and several smaller items, all fixed and re-verified.

**Milestone 3: COMPLETE, MERGED TO MAIN.** All quality gates pass, the full webhook-to-Setup pipeline (all 7 documented fixture behaviors) is verified end-to-end against live Postgres/Redis/HTTP, and independent architecture/journal-completeness/quality reviews are all resolved: one BLOCKER (a BullMQ retry could create a duplicate `Setup` from one webhook delivery), one HIGH (malformed-envelope/v1-shape rejections weren't durably persisted), and several MEDIUM/LOW findings, all fixed and re-verified. A post-completion hardening pass (webhook crash-window reconciliation, deterministic timeline ordering, an indexed health-check replacement, configurable WebSocket CORS) landed alongside Milestone 5 on branch `milestone-3-5-work` and merged into `main` together with it (fast-forward, commit `5412909`, 2026-09-23).

**Milestone 5: COMPLETE, MERGED TO MAIN.** All quality gates pass, a real screenshot was generated end-to-end through the live Playwright pipeline and confirmed (via both visual inspection and a direct query) to exclude a known future candle for PRE_TRADE and correctly bound the window at exit for POST_TRADE, and independent architecture/anti-look-ahead/journal-completeness/quality reviews are all resolved — two HIGH findings (a docs overclaim about DB-enforced immutability, and a POST_TRADE screenshot audit-trail correlation bug that made `getSetupTimeline` blind to its lifecycle events) plus three MEDIUM findings (missing cutoff-argument test coverage on the render routes, a missing retry journal event, a discarded failure message), all fixed and re-verified. Merged into `main` via fast-forward (branch `milestone-3-5-work` was 43 commits ahead with no divergence, so history is preserved commit-for-commit; `main`'s history remains linear, matching this repo's existing convention of zero merge commits). Two environment-only issues surfaced on the fresh `main` checkout post-merge and were fixed directly on `main` (not code defects in the merged work — both reproduced identically in the source worktree once its own caches were cleared): `turbo.json` now declares `globalPassThroughEnv` explicitly, since Turbo was silently dropping `DATABASE_URL`/`REDIS_URL` from spawned tasks; `apps/dashboard`'s `build` script now forces `NODE_ENV=production`, since the shared `.env`'s `NODE_ENV=development` (needed for the NestJS apps' Prisma logging) broke `next build`'s internal error-page prerendering when left set during a production build. Post-merge quality gates independently re-run clean: `pnpm lint`/`typecheck`/`test`/`build` all pass on `main` (120 api + 43 worker + 144 database tests, zero skipped, zero failures).

**Milestone 6: COMPLETE, MERGED TO MAIN.** Merged via fast-forward (branch `milestone-6-work` was 29 commits ahead with no divergence, commit `ea411d6`), followed by a post-merge technical-debt pass (branch `tech-debt-fixes`, also fast-forwarded, commit `85ab4e2`) that closed all four known limitations from Milestone 6's final review with real DB-level fixes — see "Known limitations (Milestone 6)" below for what was resolved. All quality gates pass (`pnpm lint`/`typecheck`/`test`/`build` run fresh from the worktree root, cleared `.next` first: lint 11/11, typecheck 19/19, test 19/19 workspace packages including 156 `apps/api` tests, build 11/11 producing 19 dashboard routes). A controller-run real end-to-end verification drove real Setups through the full notification pipeline (both the bounded-timeout text-only path and the screenshot-attached path, confirmed differentially by real timing and a real attached PNG), executed and closed real trades with `grossPnl`/`netPnl`/`rMultiple`/`outcome` independently hand-verified against the server's output, and independently hand-verified `mfe`/`mae` against real candle data (this run is what later motivated a closer look at the general-case candle-range logic - see the BLOCKER below). A dedicated concurrency test sweep (Task 15) added `apps/api/src/notifications/notification.service.redis.test.ts` (real Postgres+Redis, two genuinely simultaneous `requestNotification` calls converge on one row/job).

Two review passes ran: per-task reviews for all 17 plan tasks (zero BLOCKER, three HIGH - Task 4's `FAILED→QUEUED` retry-reset race, Task 7's stuck-`RETRYING` classification gap, Task 9's missing double-submit guard - all fixed and re-reviewed clean during Phase A/B), then a **final whole-branch review** (system-architect, journal-analyst, quant-engineer, quality-reviewer, run in parallel against the full branch diff) that found one real **BLOCKER** and several real HIGH/MEDIUM findings the per-task reviews couldn't see, all fixed and re-reviewed/re-verified clean before declaring the milestone complete:

- **BLOCKER (quant-engineer)**: `closeJournalTrade`'s MFE/MAE candle-range query used `timestamp >= entryTimestamp` - but a candle's timestamp is its OPEN time, and a real-world fill happens *during* a candle's interval, not at its exact open. Free-typed dashboard datetime inputs are essentially never aligned to a candle boundary, so this silently dropped the entry candle: understating MFE/MAE for multi-candle trades, and producing a spurious `null` (masquerading as "no candle data") for fast single-candle trades. The controller's own live-system verification happened to use boundary-aligned timestamps and didn't catch it - exactly the scenario the bug is invisible in. Fixed by resolving the range start to the candle that actually *contains* `entryTimestamp` (`lte`+`desc`+`findFirst`) before querying; also threaded the transaction's `tx` client through the candle read (was previously using the module-level `prisma` singleton, breaking `closeJournalTrade`'s isolation boundary). Two live-Postgres regression tests added with concrete hand-computed expected values, proving both the fix and that the old code would have produced different (wrong) numbers for the same inputs.
- **HIGH (quality-reviewer)**: `execute()`/`skip()` could produce contradictory journal history - even with Task 9's READY-status guard, nothing stopped a second `skip()`, or `skip()` then `execute()` (either order) from both succeeding, leaving one Setup marked both `SKIPPED` and executed. Fixed by broadening the existing double-submit guard (`findJournalTradeBySetupId`, was OPEN-only) to reject either action if the Setup has *any* prior recorded decision - same proportionate app-level-check tier as the existing guard, not a new DB migration.
- **HIGH (quality-reviewer)**: the dashboard displayed `mfe`/`mae` with `formatCurrency` (a "$" prefix), but `calculateExcursions` returns a raw price-point distance, never multiplied by `pointValue`/`quantity` - the same quantity the backtester computes and correctly labels as points on its own trade-detail page. Fixed by switching to `formatDecimal` labeled "points," matching the backtester page's convention. Calculation itself was always correct; this was purely a display-layer unit-mismatch bug.
- **MEDIUM (quality-reviewer, closes a previously-known-limitation)**: `NOTIFICATION_QUEUE` could still strand a row at `RETRYING` forever for a genuinely temporary-but-persistent failure (e.g. a multi-minute Telegram outage) - Task 7's `SetupContextNotFoundError` fix only closed the deterministic-permanent-failure case. Fixed: the processor now checks `job.attemptsMade` against the job's configured `attempts` and routes to `FAILED` (`RETRY_ATTEMPTS_EXHAUSTED`) on the last attempt instead of `RETRYING`.
- **MEDIUM (quality-reviewer)**: `TelegramNotificationProvider`'s `fetch()` calls had no timeout, so a hung connection could block a worker slot indefinitely. Fixed with `AbortSignal.timeout(10_000)` on both `sendMessage`/`sendPhoto`.
- **MEDIUM (journal-analyst)**: `TRADE_SKIPPED`/`TRADE_EXECUTED`/`TRADE_CLOSED` journal events carried no metadata, so a reader given only the JournalEvent stream couldn't tell *why* a trade was skipped or *what* it closed as without a second query. Fixed by adding real metadata (`skipReason`, `actualEntry`/`quantity`, `outcome`/`netPnl`/`rMultiple`) to all four emission sites.

All five fixes independently re-reviewed or controller-verified clean; final `pnpm lint`/`typecheck`/`test`/`build` re-run clean after every fix landed.

**Milestone 7: COMPLETE.** All quality gates pass fresh/uncached (`pnpm turbo run lint typecheck test build --force`: 48/48 tasks, 0 cached, 0 failures; 801 tests passing across 11 packages/apps: shared-types 96, risk-engine 52, analytics 30, strategy-engine 67, backtester 24, ai-provider 7, screenshot-storage 14, database 195 + 1 todo, worker 102 + 2 skipped, api 182, dashboard 32). A controller-run manual end-to-end verification drove a real research experiment through the actual API/worker/BullMQ/Postgres/Redis stack (not mocked): a RESEARCH-stage experiment completed in ~3 seconds with 9 real backtest trades, its `StrategyVersion` advanced `DISCOVERED → BACKTESTING`, and all expected `JournalEvent`s (hypothesis proposed, experiment created, experiment completed, status changed) were confirmed via a direct database query, not just the script's own console output.

19 plan tasks (Tasks 1-13 implementation across two parallel lanes - backend: schema/contracts/ai-provider/ResearchAgent/BullMQ-processor/api-module/AnthropicAIProvider; frontend: research list + hypothesis detail pages - plus a controller-owned integration task; Tasks 14-17 four parallel final whole-branch reviews; Task 18 a three-part fix wave; Task 19 this documentation pass), every task individually reviewed by a fresh subagent before the next began, zero task carried forward with an open BLOCKER/HIGH. The four **final whole-branch reviews** (research-methodologist, journal-analyst, system-architect, quality-reviewer, run in parallel against the full branch diff since `main`, the level individual task reviews structurally can't see) found real, substantial gaps the per-task reviews missed - most consequentially, **the experiment pipeline was never actually wired end-to-end**: `createExperiment` created a `Backtest` row but never enqueued it, and nothing called the experiment-completion functions, so in production a `ResearchExperiment` could never leave `QUEUED` and `PAPER_CANDIDATE` was unreachable through the real system. This was independently confirmed by three different reviewers (research-methodologist's BLOCKER, quality-reviewer's HIGH, and system-architect's own check) before being fixed in a three-part fix wave:

- **Fix-wave-1** (the pipeline wiring itself): `ResearchService.createExperiment` now enqueues onto the existing `BACKTEST_RUN_QUEUE`/`BacktestRunProcessor` (no duplicated backtest logic); the processor's completion/failure hooks advance the linked `ResearchExperiment` and its hypothesis's `StrategyVersion` status, with the completion hook deliberately running outside the backtest's own try/catch so a research-bookkeeping bug can never relabel a genuinely-successful backtest as failed. Also closed a real TOCTOU the fix's own first draft left open (the final-test-reuse rule only fires on INSERT, so a read-only precondition pre-check alone wasn't race-safe; closed with a transaction that re-checks authoritatively) - independently proven race-safe by the review's own live 5-round, 6-concurrent-request test against real Postgres (exactly one winner per round, zero orphan `Backtest` rows). Added dataset-window isolation between a hypothesis's stages and a holdout-date guard (neither existed before this fix - every stage could previously reuse an identical window). Fixed `markResearchExperimentFailed` to be an atomic conditional update (was a blind `update()`, which could have let a `COMPLETED` final test be relabeled `FAILED` to bypass the non-renewable-final-test database constraint). A migration widened the final-test index's status set and changed `ResearchExperiment`'s foreign keys from cascade-delete to `RESTRICT`. Surfaced and safely resolved one operational issue along the way: an earlier, unrelated commit had edited a comment inside an already-applied migration, breaking `prisma migrate dev`'s checksum check - fixed by correcting the one affected `_prisma_migrations` metadata row (verified via independent SHA-256 recomputation), never by resetting the database.
- **Fix-wave-2** (a serious, independently-confirmed test-quality gap): the DSL interpreter's "is look-ahead-safe" test - CLAUDE.md's single highest-priority test category - used a fixture that produced zero trading signals in both the full and truncated candle series, so its core assertion compared `[]` to `[]` and could never fail regardless of whether the interpreter was actually safe. This directly contradicted an earlier task review's claim that it was a genuine, verified proof (that review reasoned correctly about the test's *structure* but never checked it produced a nonzero signal count). Replaced with genuinely signal-producing, hand-verified fixtures for all four DSL rule types, each proving both truncation-invariance and future-candle-mutation-invariance - and the fix was proven correct twice, independently, by two different reviewers temporarily injecting a real look-ahead bug into the interpreter and confirming the new tests actually caught it, then reverting. Also added `STRATEGY_DEFINITION_BOUNDS` (ATR multipliers `[0.25, 10]`, all periods `[1, 500]`) closing a parameter-mining risk, and fixed the equally-vacuous DSL-backed backtest regression test the same way.
- **Fix-wave-3** (AI-provider audit integrity): a `FAILED` `AgentExecution` previously discarded its raw response, tokens, and cost even when the provider call was genuinely billed (a schema-invalid tool response, or a successful call whose later persistence failed) - fixed with a new `AIProviderResponseError` carrying the full audit trail through both failure paths, so `getTodayResearchSpend` no longer undercounts a failed-but-billed call. Fixed a real audit-integrity gap where the `AgentExecution` row recorded the API process's pre-guessed provider/model, not the value the worker process's provider *actually* reported - proven to matter with a live forced-401 test against a real (invalid-key) Anthropic call, which previously got misattributed as MOCK. Fixed `attempts: 1` being wired onto the wrong BullMQ `Queue` instance (the worker's registration, not the API's, which is the one that actually calls `.add()`). Fixed `RESEARCH_DAILY_TOKEN_BUDGET` silently disabling the budget on a malformed value (`NaN >= budget` is always `false` in JavaScript) - now fails closed via Zod validation. Removed an unchecked `as never` cast at the AI-provider boundary.

Two smaller findings were fixed directly by the controller rather than a subagent, given their trivial single-file scope: `RESEARCH_HYPOTHESIS_CREATED`/`RESEARCH_EXPERIMENT_CREATED` journal events were confirmed genuinely missing (journal-analyst's own review), the former fixed by the reviewer itself with live-database verification, the latter moved into its repository transaction after a follow-up review found it was still written outside it; and the controller's own Task 13 manual-verification script was found to bypass `AgentExecutionProcessor` entirely, silently producing zero journal events despite its doc comment claiming to verify the pipeline "end to end" - fixed to construct and call the real processor, re-verified against real database rows.

All fixes independently re-reviewed (three separate review rounds, one per fix wave) with zero residual BLOCKER/HIGH before the milestone was declared complete. See "Known limitations (Milestone 7)" below for what was deliberately deferred, and why.

## Milestone 1 — Completed

- Claude Code configuration: root `CLAUDE.md`, `.claude/agents/*.md` (system-architect, quant-engineer, data-engineer, backend-engineer, frontend-engineer, platform-engineer, research-methodologist, journal-analyst, quality-reviewer).
- Workspace scaffolding: pnpm workspace, Turborepo pipeline, root TypeScript/ESLint/Prettier config, `.env.example`, `infra/docker-compose.yml` (Postgres 16 + Redis 7).
- `packages/shared-types`: enums, CSV candle schema, instrument/backtest request schemas.
- `packages/trading-domain`: Milestone 1 entity interfaces + forward-declared future-milestone interfaces.
- `packages/database`: Prisma schema/migration, streaming CSV importer, repositories, seed data — verified against a live Postgres (migration applied, seed idempotency confirmed, CSV import smoke-tested).
- `packages/strategy-engine`: EMA/ATR indicators (explicit warm-up), EMA Trend Pullback v1.0.0 — 28 tests.
- `packages/risk-engine`: deterministic sizing/risk math, rejects invalid input — 34 tests.
- `packages/backtester`: deterministic backtest engine + metrics (next-bar entry, conservative same-candle stop/target, slippage/commissions/MFE/MAE) — 22 tests, including a determinism check.
- `apps/api`: NestJS (Health/Instrument/MarketData/Strategy/Backtest modules), Zod validation pipe, BullMQ producer — verified end-to-end against live Postgres/Redis (created and completed a real backtest via HTTP, 33 trades + computed metrics; a second identical run produced byte-identical trades/metrics).
- `apps/worker`: BullMQ consumer running `packages/backtester`, idempotent on retry (transactional replace).
- `apps/dashboard`: Next.js research UI — Dashboard/Research/Strategies/Backtests/Market Data nav, create-backtest form, live status polling, metrics/trades table, trade detail with surrounding candles, CSV import form. Verified rendering against the live API via headless Chromium.
- Documentation: `docs/architecture.md`, `docs/backtesting-assumptions.md`, `docs/research-methodology.md`, `docs/trade-journal-design.md`, `docs/screenshot-design.md`, `docs/roadmap.md`, `README.md`.
- Final review pass: system-architect (no blockers), research-methodologist (no blockers; independently re-derived the backtest result to confirm it's honest and untuned), quality-reviewer (GO; 0 BLOCKER/HIGH, 1 MEDIUM + 2 LOW, all addressed — see "Known decisions").

## Milestone 2 — Completed

- `packages/shared-types`: `SetupStatus`/`SetupSource`/`ExecutionMode`/`JournalTradeStatus`/`TradeSource`/`PostTradeOutcome`/`LossCategory`/`JournalEventType`/`JournalEntityType`/`ScreenshotType` enums; Zod DTOs for creating/transitioning setups, risk calculations, and journal trades (`journal.ts`) — 18 new tests.
- `packages/trading-domain`: `MarketSnapshot`/`Setup`/`RiskCalculation`/`JournalTrade`/`JournalEvent`/`PostTradeAnalysis`/`TradeScreenshot`/`NormalizedTrade` promoted to real entities (`journal-entities.ts`); `Signal`/`AgentExecution`/`TradeTicket` remain in `future.ts` for Milestone 3+.
- `packages/risk-engine`: added `calculateGrossPnl`/`calculateNetPnl`/`calculateRMultiple` — 10 new tests (47 total).
- `packages/database`: migration `20260917161439_add_journal_analytics_foundation` (7 new tables), repositories for every Milestone 2 model (Setup's transition matrix fully validated, journal events emitted atomically alongside every state change), `getNormalizedTrades()` backtest↔journal analytics adapter, one demo Setup lifecycle added to the seed script — verified end-to-end against live Postgres via a full integration test (setup → snapshot → risk calc → journal events → paper trade → close → timeline → normalized-trade analytics).
- `packages/analytics` (new): `calculateTradeAnalytics`, `groupTradeAnalytics` (`DEFAULT_GROUP_BY` never silently merges strategy versions), `compareWinnersLosers` — zero database dependency, 22 tests.
- `apps/api`: `MarketSnapshotModule`, `SetupModule`, `JournalModule`, `AnalyticsModule`; a global `DomainErrorFilter` translates repository errors to 404/409 — verified end-to-end against live Postgres/Redis with real numbers (105 normalized trades merging 104 Milestone 1 backtest trades + 1 new journal trade).
- `apps/dashboard`: `/journal`, `/trades`, `/analytics` pages + nav update — verified against the live API (real 404 handling, real numbers matching the raw API response, a real demo trade's 7-event setup timeline rendered in order).
- Final review pass: system-architect (no blockers; 2 LOW items, both fixed), journal-analyst (no integrity issues; corrected 2 doc inaccuracies and found the `STRATEGY_VERSION_PROPOSED` gap below, now closed), research-methodologist (no causal-language/null-handling/silent-merge issues; 1 MEDIUM — `/analytics` lacked a trust disclaimer — fixed), quality-reviewer (1 HIGH — `getNormalizedTrades()` wasn't chronologically ordered, corrupting drawdown/streak metrics — fixed and re-verified; everything else clean).

## Milestone 3 — Completed

- `packages/shared-types`: `tradingview.ts` (envelope/v1 Zod schemas, deterministic SHA-256 fingerprint, TradingView-interval-to-`Timeframe` mapping, `normalizeTradingViewPayload`, decimal-based candle invariant checks), `realtime.ts` (`REALTIME_CHANNEL`, discriminated-union `RealtimeEvent` schema), `setup-expiration.ts` (BullMQ queue contract), 111 new tests across the package. New `WebhookProvider`/`WebhookProcessingStatus`/`WebhookFailureCode`/`TradingViewSignalType` enums and `SetupSource: "TRADINGVIEW"`, `JournalEventType`/`JournalEntityType` additions.
- `packages/trading-domain`: `InboundWebhookEvent`/`TradingViewInstrumentMapping`/`NormalizedSignal` entities; `Setup.plannedStop`/`plannedTarget1` made nullable (a TradingView-sourced setup often begins with only a candidate entry) and `Setup.sourceWebhookEventId` added (idempotency, see below).
- `packages/database`: migrations `20260917192801_add_tradingview_webhook_ingestion` and `20260918053000_add_setup_source_webhook_event_id`; `inbound-webhook-events.ts` and `tradingview-instrument-mappings.ts` repositories, `findStrategyVersionByKeyAndVersion`, `findSetupBySourceWebhookEventId`, `SetupIncompletePlanError`, verified end-to-end against live Postgres, including a genuine `Promise.all` concurrency test proving the fingerprint's database `@unique` constraint (not check-then-insert) makes duplicate delivery race-safe.
- `apps/api`: `POST /webhooks/tradingview` (envelope/v1 validation funnel, rate-limited via `@nestjs/throttler`, all rejection paths durably persisted), `GET /webhooks/tradingview/events[/:id[/timeline]]` admin/inspection endpoints, a WebSocket realtime gateway (`@nestjs/websockets` + socket.io) forwarding Redis pub/sub events, `GET /health` extended with real `tradingViewIngestion` status.
- `apps/worker`: `TradingViewWebhookProcessor` (normalize → resolve instrument/strategy → `MarketSnapshot` → `Setup` → journal, retry-safe via `Setup.sourceWebhookEventId`'s uniqueness), `SetupExpirationProcessor` (delayed BullMQ job, never overwrites a terminal status), a shared realtime publisher. Both new queues configured with `attempts: 3` + exponential backoff.
- `apps/dashboard`: `/live-setups` (live table + WebSocket, reconnect reloads from REST API), `/setups/:id` (new Setup detail page; none existed before), `/webhook-events[/:id]` admin/inspection pages, nav update.
- Also fixed in this milestone: apps/api's and apps/worker's `dev` scripts were silently broken (`tsx watch`'s esbuild transpilation doesn't emit the TypeScript decorator metadata NestJS's dependency injection depends on, so any implicitly-injected constructor-based provider read as `undefined` at request time). Switched to `tsc --watch` + `node --watch dist/main.js`. This predated Milestone 3 and was not previously caught because prior milestones' manual verification happened to exercise only explicitly-tokened providers or compiled builds.
- Final review pass: system-architect (no BLOCKER/HIGH; 2 MEDIUM: missing retry policy, fixed; a narrow ingestion-crash window left as a documented known limitation), journal-analyst (1 BLOCKER: a retry could create a duplicate `Setup`, fixed with a `Setup.sourceWebhookEventId` unique constraint mirroring the existing fingerprint pattern; 1 HIGH: malformed rejections weren't persisted, fixed; smaller findings on a dead/unreachable `DUPLICATE` status and timeline-merge documentation, corrected), quality-reviewer (1 MEDIUM: bare `Number()` in a candle-invariant check inconsistent with the project's Decimal discipline, fixed; GO otherwise).
- **Post-completion hardening pass** (subagent-driven, 4 tasks, each with a task review and one fix round): `JournalEvent.sequence` (a monotonic autoincrement column) makes timeline ordering deterministic regardless of clock precision, replacing incidental `Array.prototype.sort` stability; `GET /health`'s `tradingViewIngestion` check now uses two dedicated `take: 1` indexed queries (`findMostRecentInboundWebhookEvent`/`findMostRecentProcessedInboundWebhookEvent`, backed by a new plain `@@index([receivedAt])` plus the existing compound `[processingStatus, receivedAt])` index — a task review caught that the compound index alone couldn't serve the unfiltered query, confirmed via live `EXPLAIN ANALYZE`) instead of listing every `InboundWebhookEvent` row; `WebhookReconciliationProcessor` (`apps/worker`) closes the narrow crash window between an `InboundWebhookEvent` being persisted and its BullMQ job being enqueued, via a periodic `Queue.upsertJobScheduler` sweep — a task review found the naive re-enqueue could race with a still-unprocessed original job and corrupt the audit-trail status, fixed by giving both enqueue call sites (the original in `tradingview-webhook.service.ts` and the reconciliation re-enqueue) the same deterministic `jobId: event.id`, so BullMQ's own dedup makes at most one job per event possible at a time; `REALTIME_CORS_ORIGIN` makes the WebSocket gateway's CORS origin configurable for production while staying permissive by default in local development. See `docs/tradingview-setup.md` "Known limitations" for the updated status of each.

## Milestone 5 — Completed

- `packages/shared-types`: `screenshot.ts` (`CHART_CONFIG_VERSION`, render dimensions, `RENDER_READY_TIMEOUT_MS`, `SCREENSHOT_QUEUE`/job-name constants, `ScreenshotGenerationJobPayload`, the trigger endpoints' body schemas).
- `packages/database`: migration adding `ScreenshotStatus` and the real `TradeScreenshot` lifecycle columns (`status`, `storageProvider`, `renderedAt`, `failureCode`/`failureMessage`) plus the two `@@unique` idempotency/immutability constraints (`[setupId, type, chartConfigVersion]`, `[tradeId, tradeSource, type, chartConfigVersion]`); `trade-screenshots.ts` repository (`requestOrRetryScreenshot`, and `markScreenshotGenerating`/`markScreenshotReady`/`markScreenshotFailed` rewritten from read-then-update to atomic conditional `updateMany` guards after a reproduced concurrency bug let two racing calls both succeed against one row); `candles.ts`'s `getCandlesUpToTimestamp` is the SQL-level anti-look-ahead enforcement point both render routes depend on; `[status, renderedAt]` and `[status, updatedAt]` indexes backing `GET /health`'s screenshot checks.
- `packages/screenshot-storage` (new): `ScreenshotStorage` interface, `LocalDiskScreenshotStorage` (atomic temp-write-then-rename, path-traversal-guarded), `buildScreenshotStorageKey`. No dependency on anything else in the monorepo.
- `apps/worker`: `BrowserManager` (one shared headless Chromium instance, lazily launched, context-per-job), `ScreenshotGenerationProcessor` (the full render→capture→store→mark-ready flow, job-type/screenshot-type consistency check, `NAVIGATION_FAILED` vs `RENDER_TIMEOUT` vs page-reported error codes, `BrowserContext` closed — not just the `Page` — after every job to prevent a real context leak that was caught and fixed during this milestone), its own `ScreenshotStorage` provider registration.
- `apps/api`: `ScreenshotModule`/`ScreenshotController`/`ScreenshotService` (`GET /screenshots/:id/image` serving PNG bytes through a controlled route — never the storage directory directly; `POST /setups/:id/screenshots/pre-trade`, `POST /journal/trades/:id/screenshots/post-trade`, `GET /setups/:id/screenshots`, `GET /journal/trades/:id/screenshots`); auto-trigger wiring in `SetupService.updateStatus` (on reaching `READY`) and `JournalTradeService.close`, both best-effort and never failing the operation they're attached to; a job-state-aware `enqueueJobIfNeeded` helper (mirroring the Milestone 3 hardening pass's webhook-reconciliation pattern) that fixes a real regression where a retried `FAILED→REQUESTED` row's re-enqueue silently no-op'd against BullMQ's still-retained failed job; `GET /health` extended with `screenshotGeneration` (`HEALTHY`/`DEGRADED`/`UNKNOWN`, derived from real `TradeScreenshot` rows) and `screenshotStorage` (`up`/`down`, a real write+read+delete probe against the configured backend) fields.
- `apps/dashboard`: `/internal/render/setup/[setupId]` and `/internal/render/trade/[tradeId]` (server-only, Playwright-only render routes; the PRE_TRADE route is structurally proven, via static-analysis tests, to never import or call any journal/trades endpoint), `RenderClient`/`ChartRenderer`, `lib/render-ready.ts` (the DOM ready/error contract), `ScreenshotCard`/`ScreenshotStatusBadge` wired into `/setups/:id` and `/trades/:id` with a working retry action, a lazily-fetched screenshot column on `/live-setups`.
- Testing: `packages/database`'s `candles.integration.test.ts` (exact-cutoff-included, 1ms-after-excluded, truncation direction) and `screenshot-immutability.integration.test.ts` (PRE_TRADE untouched after trade close, POST_TRADE never overwrites PRE_TRADE, a `chartConfigVersion` bump creates a new row without touching the old one, duplicate requests collapse to one row under both real sequential and genuinely concurrent conditions); `apps/dashboard`'s `route-boundaries.test.ts` (static-analysis proof of the PRE_TRADE route's anti-look-ahead import boundary, including a check that the guard itself is not vacuous); unit coverage across the processor, browser manager, storage, key generation, screenshot service (including a dedicated Redis-backed test proving the failed→retry re-enqueue path against a real BullMQ queue, not a mock), and health service.
- Documentation: `docs/screenshot-design.md` rewritten to describe the shipped pipeline; `docs/trade-journal-design.md`'s "Screenshots" section updated; `docs/architecture.md` updated with `packages/screenshot-storage` and the two internal render routes; `docs/roadmap.md` and this file updated; `README.md`'s quickstart updated with the Playwright browser-install step.
- Real end-to-end verification (controller-run, not delegated): seeded real candles with a distinctive future marker, sent a real TradingView webhook through the live API, drove a Setup to `READY`, and confirmed via the real Playwright pipeline that the resulting PRE_TRADE PNG excludes the future candle (visually — no price-axis distortion from the marker value); confirmed idempotency (a repeat request returns the identical row); closed a real `JournalTrade` and confirmed the resulting POST_TRADE PNG both includes the marker candle (now legitimately inside the entry→exit window) and, via a direct query against `getCandlesUpToTimestamp` with the trade's real `exitTimestamp`, excludes the candle immediately after exit; confirmed the PRE_TRADE row's `renderedAt`/`updatedAt` are byte-identical before and after the trade closed. This run also surfaced a critical bug: `SCREENSHOT_STORAGE_ROOT` (a relative path) resolved against each Node process's own `process.cwd()`, which differs between `apps/api` and `apps/worker` when started via `pnpm --filter <pkg> start` — the worker wrote screenshots to a directory the API could never find. Fixed with `resolveScreenshotStorageRoot` (packages/screenshot-storage), which anchors a relative root to the monorepo root via a `pnpm-workspace.yaml` marker walk-up instead of `process.cwd()`; all three call sites (api provider, worker provider, and a third in `health.service.ts` the fix uncovered) updated, with a dedicated `process.chdir()`-based regression test.
- Final whole-branch review (system-architect, research-methodologist, journal-analyst, quality-reviewer — all four independently run against the full 39-commit diff, not sequential per-task reviews): zero BLOCKER findings from any reviewer. Two HIGH findings from journal-analyst, both fixed: `docs/screenshot-design.md`/`docs/trade-journal-design.md` overclaimed that the two `@@unique` constraints make a `READY` row's fields DB-immutable — they only prevent a second row, not an `UPDATE` on an existing one; immutability is actually a convention-based guarantee (no exposed update path touches a `READY` row), the same honest pattern `JournalEvent` already documents — docs reworded to match. And POST_TRADE screenshot journal events correlated onto the screenshot's own id instead of its originating Setup's id (`setupId` is always null for a POST_TRADE row), making `getSetupTimeline` blind to POST_TRADE screenshot lifecycle events — fixed with a denormalized `correlationSetupId` column, migration `20260923090605_add_screenshot_correlation_setup_id`. Three MEDIUM findings, all fixed: research-methodologist found no test asserted the actual cutoff argument each render route passes to `getCandlesUpToTimestamp` (only the shared query primitive's boundary was tested, not that each call site invokes it correctly) and no static regression guard existed for the POST_TRADE route at all — fixed with component-level tests asserting the exact mocked cutoff value per route plus a POST_TRADE `route-boundaries.test.ts` mirroring the existing PRE_TRADE one; journal-analyst found a `FAILED → REQUESTED` retry emitted no `JournalEvent` at all (added `SCREENSHOT_RETRIED`) and that a retry silently discarded the prior failure's `failureMessage` with no journal record of it either (now captured in `markScreenshotFailed`'s event metadata). Independently re-run by the controller after all fixes landed: `pnpm lint`/`typecheck`/`test`/`build` all pass (0 errors; one pre-existing unrelated lint warning in `packages/database/src/mappers.test.ts`).

## Milestone 6 - Completed

- `packages/database`: migration adding the `NotificationDelivery` model (`@@unique([setupId, notificationType, templateVersion])`, a real Postgres unique constraint, confirmed in the migration SQL, not check-then-insert), `SkipReason` enum, `JournalTrade.skipReason`/`outcome` columns, and 5 new `NOTIFICATION_*` `JournalEventType` values plus a `NOTIFICATION_DELIVERY` `JournalEntityType`. New `notification-deliveries.ts` repository: `requestOrRetryNotification` (idempotent create via the unique constraint, `P2002`-caught and re-read on a race, a `FAILED→QUEUED` retry-reset branch that was found unguarded by a HIGH-severity task review - a plain `update()` keyed only on `id` let two genuinely concurrent retries of the same `FAILED` row both "win" and both enqueue - fixed with an atomic conditional `updateMany({where:{id,status:"FAILED"}})`), `markNotificationSending`/`Sent`/`Failed`/`Retrying` (all atomic conditional `updateMany` guards, mirroring the Milestone 5 `TradeScreenshot` pattern), each emitting its matching `JournalEvent` in the same operation. `closeJournalTrade` now computes `mfe`/`mae` server-side via `packages/risk-engine`'s new `calculateExcursions` over the real candle range between entry and exit (`null` when the trade has no `Setup` lineage) and derives `outcome` (`WIN`/`LOSS`/`BREAKEVEN`) purely from `netPnl`'s sign - `closeJournalTradeSchema` no longer accepts client-supplied `mfe`/`mae` at all.
- `packages/risk-engine`: `calculateExcursions(candles, entryPrice, direction)` - verified line-by-line during task review against `packages/backtester`'s `walkTradeForward` favorable/adverse formula to confirm no sign flip, off-by-one, or rounding divergence; `decimal.js` throughout, throws (never fabricates a 0/0 result) on an empty candle array or non-finite input.
- `packages/shared-types`: `notifications.ts` (`NOTIFICATION_QUEUE`/`SEND_NOTIFICATION_JOB`, `NOTIFICATION_TEMPLATE_VERSION`, `NOTIFICATION_SCREENSHOT_WAIT_MS: 8_000`/`NOTIFICATION_SCREENSHOT_POLL_INTERVAL_MS: 500`, `executeSetupSchema`/`skipSetupSchema`/`notificationListQuerySchema`, `isTelegramConfigured` - the single shared implementation of the three-Telegram-env-var check, used by both `apps/worker`'s provider factory and `apps/api`'s health check); new `NOTIFICATION_TYPES`/`NOTIFICATION_PROVIDER_TYPES`/`NOTIFICATION_DELIVERY_STATUSES`/`SKIP_REASONS` enum-value arrays, field-matched against the Prisma enums.
- `apps/worker`: `NotificationProvider` interface plus `ConsoleNotificationProvider` (sanitized log output, synthetic `externalMessageId`, used by every automated test) and `TelegramNotificationProvider` (plain `fetch`, `sendMessage`/`sendPhoto`, bot token never logged or embedded in a thrown error's message, a fixed `classify()` failure-code mapping so raw provider text never reaches a stored `failureMessage`); `notification-provider.factory.ts` (credential-free console fallback on any missing/partial Telegram config, never throws at startup); `ready-trade-card.ts` templates (a typed `SetupStatusNoticeData`/`ReadyTradeCardData` object, no `Decimal` input, confirmed `decimal.js`-free); `NotificationSendProcessor` (resolves Setup context via the owning repositories since `setupsRepository.getSetup` returns no relations, formats the message, runs the bounded screenshot wait, calls the provider, classifies TEMPORARY/PERMANENT/`SetupContextNotFoundError` outcomes - see the HIGH finding above); `NOTIFICATION_QUEUE` registered in `app.module.ts` with `attempts: 5` and exponential backoff (distinct from the screenshot queue's `attempts: 1`).
- `apps/api`: `NotificationService`/`enqueueNotificationIfNeeded` (job-state-aware BullMQ enqueue, mirroring the screenshot-service pattern exactly - `getJob`→branch→`retry("failed")`, never a blind `add()`); `SetupService.notifyForStatus` (the notification policy - see `docs/notifications.md`), wired into `updateStatus` as a best-effort, never-fails-the-transition side effect; `GET /setups/:id/notifications` (thin read endpoint, mirrors `listRiskCalculations`); `POST /setups/:id/execute` and `/skip` (`SetupService.execute`/`skip` - see the Task 9 HIGH finding above for the double-submit guard); `GET /health` extended with a `notifications` check (`DISABLED`/`UNKNOWN`/`DEGRADED`/`HEALTHY`, confirmed by task review to require a genuine `SENT` row before ever reporting `HEALTHY`, not merely that credentials are configured).
- `apps/dashboard`: `/setups/:id` gains `SetupActionsCard` (PAPER TRADE / I ENTERED THIS TRADE / SKIP TRADE forms with client-side validation matching `executeSetupSchema`/`skipSetupSchema`, a `409` from a double-submit surfaced as a clear message) and `NotificationsCard` (read-only `NotificationDelivery` status list); `/trades/:id` gains `CloseTradeCard` (collects exactly `closeJournalTradeSchema`'s fields - no `mfe`/`mae` inputs) and an `OutcomeBadge` (`WIN` green/`LOSS` red/`BREAKEVEN` gray, reads the server's `outcome` directly, never derives it client-side). Both task reviews independently confirmed zero client-side financial arithmetic anywhere in the diffs (only formatting/sign-extraction via pre-existing helpers).
- Testing: `apps/database`'s `notification-idempotency.integration.test.ts` (10 concurrent `requestOrRetryNotification` calls against live Postgres converge on exactly one row); `apps/api`'s `notification.service.redis.test.ts` (Task 15, real Postgres+Redis, two genuinely simultaneous `requestNotification` calls converge on one `NotificationDelivery` row and one BullMQ job, no double-enqueue against an already-`QUEUED` row); unit coverage across both `NotificationProvider` implementations (including explicit `.not.toContain` assertions proving the bot token never leaks into a thrown error even when the underlying fetch rejection itself would have contained it), the processor (including a regression test for the `SetupContextNotFoundError` fix), the templates, `NotificationService`, `SetupService`'s policy and execute/skip guards, and the dashboard forms.
- Documentation: `docs/notifications.md` (new), `docs/trade-journal-design.md`'s "Skip workflow" section and MFE/MAE/`outcome` language, `docs/architecture.md`'s notification module boundaries, `docs/roadmap.md` and this file, `README.md`'s Telegram setup instructions.
- Final review pass: task-level reviews for all 17 tasks (Tasks 1-13 implementation, 14-17 integration/verification/docs), zero BLOCKER at that level, three HIGH (Task 4's `FAILED→QUEUED` retry-reset race, Task 7's stuck-`RETRYING` classification gap, Task 9's double-submit guard), all fixed and re-reviewed clean before the next dependent task started. A subsequent **final whole-branch review** (system-architect, journal-analyst, quant-engineer, quality-reviewer, run in parallel against the full 28-commit branch diff, a level individual task reviews structurally can't see) found one real BLOCKER (the MFE/MAE candle-range bug, see above) and several real HIGH/MEDIUM findings (execute/skip contradictory-history guard, MFE/MAE currency-display bug, notification `RETRYING`-exhaustion strand, Telegram fetch timeout, journal-event metadata) - all fixed and re-reviewed/re-verified clean. See `.superpowers/sdd/2026-09-23-milestone-6-notifications/progress.md` for the full per-task and final-review ledger.

## Milestone 7 - Completed

- `packages/strategy-engine`: `types.ts` (`StrategySignal`, moved out of `ema-trend-pullback.ts` once a second strategy needed it), `strategies/strategy-definition.ts` (the closed DSL: `strategyDefinitionSchema`, four entry-rule types (`EMA_CROSS_ABOVE`/`EMA_CROSS_BELOW`/`CLOSE_ABOVE_EMA`/`CLOSE_BELOW_EMA`) and `evaluateStrategyDefinition`, the deterministic interpreter, look-ahead-safe by construction and proven so with genuinely signal-producing fixtures per rule type covering both truncation- and future-mutation-invariance; `STRATEGY_DEFINITION_BOUNDS` bounding ATR multipliers to `[0.25, 10]` and every period to `[1, 500]`), `registry.ts` (`STRATEGY_REGISTRY` generalized to include `"ai-generated-dsl-v1"` alongside the existing `"ema-trend-pullback"`, with `ema-trend-pullback`'s own behavior/13-test suite confirmed byte-for-byte unchanged), 67 tests.
- `packages/backtester`: `runBacktest`/`BacktestRunInput` widened to be generic over any registered `StrategyKey` (was hardcoded to `EmaTrendPullbackParameters`), with exactly two honestly-documented structural-narrowing casts (both runtime-safe, since every registered evaluator re-validates its own parameters via its own Zod schema) and an optional compile-time guard proving every registry entry's parameters extend `AtrStopTargetParameters`, 24 tests.
- `packages/analytics`: `research-summary.ts` (`buildResearchDataSummary`, the deterministic statistics layer the `ResearchAgent` consumes, never raw candles; `assertSampleSizeGuardrails`, a real AND, both a calendar-day and a candidate-setup minimum required, never either alone), `parameter-sensitivity.ts` (`compareParameterVariations`, an isolated-performance-peak warning), `walk-forward.ts` (`generateWalkForwardWindows`, a pure window-generation foundation), 30 tests.
- `packages/database`: migration `20260923153300_add_research_agent_framework` (`AgentExecution`/`ResearchHypothesis`/`ResearchExperiment` models, `PAPER_CANDIDATE` added to `StrategyVersionStatus`, the final-test-reuse partial unique index) plus a follow-up migration `20260924090000_harden_research_experiment_history` (RESTRICT foreign keys, widened final-test index status set, `RESEARCH_EXPERIMENT_FAILED`); `research.ts` repository (`AgentExecution`/`ResearchHypothesis`/`ResearchExperiment` CRUD, all status transitions atomic conditional updates with their `JournalEvent` in the same transaction, dataset-window-isolation and holdout-date preconditions, `getTodayResearchSpend` budget accounting); `strategies.ts`'s `advanceStrategyVersionStatus` (monotonic-only, capped at `WALK_FORWARD`) and `markPaperCandidate` (atomic, ownership-checked, guardrail-checked), 195 tests + 1 todo, all live-Postgres-verified including a real 5-round concurrent-creation race test.
- `packages/ai-provider` (new package): `AIProvider` interface, `MockAIProvider` (deterministic hash-seeded output, zero network, the default whenever `ANTHROPIC_API_KEY` is unset, including every automated test), `AnthropicAIProvider` (real provider, forced tool-call, Zod-validated response, `AIProviderResponseError` preserving raw response/tokens/cost on both billed-but-unusable failure paths), `researchHypothesisOutputSchema`, zero dependency on `packages/database`/NestJS/BullMQ/`packages/analytics`, confirmed by the final architecture review, 7 tests.
- `apps/worker`: `research/research-agent.ts` (`ResearchAgent`, pure orchestration + JSON-safe Decimal-to-string serialization, no BullMQ/Prisma dependency, unit-testable with `MockAIProvider` alone), `research/agent-execution.processor.ts` (`AgentExecutionProcessor`, drives an `AgentExecution` through the real `AIProvider`, persists the outcome, records `AGENT_STARTED`/`AGENT_COMPLETED`/`AGENT_FAILED`/`RESEARCH_HYPOTHESIS_CREATED` journal events, preserves a billed failure's audit trail), `research/ai-provider.factory.ts` (`createAIProviderFromEnv`); `backtest-run/backtest-run.processor.ts` extended (not duplicated) to drive a research-originated `Backtest` to `ResearchExperiment` completion/failure and advance its `StrategyVersion` status, with the research-bookkeeping hook running outside the backtest's own try/catch so a bookkeeping bug can never relabel a successful backtest as failed, 102 tests + 2 skipped.
- `apps/api`: `research/research.module.ts`/`research.controller.ts`/`research.service.ts` (`POST /research/hypotheses/generate`, `GET /research/hypotheses[/:id]`, `POST /research/hypotheses/:id/experiments`, `POST /research/hypotheses/:id/strategy-versions/:versionId/mark-paper-candidate`; daily token-budget enforcement that fails closed on a malformed env value; `createExperiment` enqueues onto the same `BACKTEST_RUN_QUEUE`/`BacktestRunProcessor` `POST /backtests` uses, no duplicated logic; `markPaperCandidate` requires `confirmedByHuman: true`, a completed `FINAL_TEST`+`WALK_FORWARD` pair, ownership between the hypothesis and the `StrategyVersion` being promoted, and a passing sample-size guardrail over the combined evaluation window); `domain-error.filter.ts` extended to map `FinalTestAlreadySpentError`/`ResearchStageOrderError`/`ResearchBudgetExceededError` to 409, 182 tests.
- `apps/dashboard`: `/research/hypotheses` (list page) and `/research/hypotheses/[id]` (detail page: statement/rationale, the proposed `StrategyDefinition` and source data summary as raw JSON, experiment history table, `AgentExecution` audit detail including a raw-response disclosure), moved from the plan's originally-specified `/research`/`/research/[id]` routes after discovering `apps/dashboard` already had an unrelated Milestone 1 backtest-research page at `/research` with its own `/research/[backtestId]` dynamic route, which would have both been silently destroyed and collided with Next.js's one-dynamic-segment-name-per-parent rule; confirmed via a real production build showing all four `/research`-prefixed routes coexisting with zero collision. `StatusBadge.tsx` gained four new badge components for the new enums, 32 tests (dashboard-wide).
- Testing: a new `packages/database/src/research-pipeline.integration.test.ts` (live Postgres: window isolation, the holdout guard, the transactional Backtest+experiment create, atomic experiment-lifecycle transitions, the monotonic `StrategyVersion` status advance, a genuine 5-round/6-concurrent-request final-test race proving the TOCTOU fix); `packages/strategy-engine/src/strategies/strategy-definition.test.ts`'s look-ahead-safety proof, independently reproduced twice by injecting a real bug into the interpreter and confirming the tests catch it; `packages/backtester/src/engine.test.ts`'s DSL-through-`runBacktest` regression test, hand-verified entry/stop/target/exit/sizing/P&L/MFE-MAE and determinism-on-rerun.
- Documentation: `docs/ai-research.md` (new, the reference for the whole pipeline: the `AIProvider` abstraction, why a DSL instead of generated code, the `AgentExecution` audit model, the dataset-role pipeline and final-test safeguard, experiment budgets, what the `ResearchAgent` can/cannot see, how `PAPER_CANDIDATE` is reached, known limitations), `docs/research-methodology.md` (a "How the Milestone 7 experiment pipeline enforces this" section added by the fix-wave-1 implementer, plus `PAPER_CANDIDATE`'s place in the strategy-lifecycle diagram), `docs/architecture.md` (`packages/ai-provider`'s isolated dependency footprint, the `packages/database → packages/analytics` edge Milestone 7 introduced, new `apps/api`/`apps/worker`/`apps/dashboard`/`packages/database` bullets, a new "Reproducibility and auditability" entry for the final-test-reuse/atomic-transition guarantees), `docs/roadmap.md` (Milestone 7 marked complete under its chronological-shipping-order number, with cross-references reconciling the roadmap's original placeholder titles for slots 6/7/9/11, see that file for the full explanation), this file.
- Final review pass: 19 plan tasks, every one individually reviewed before the next began (zero task left with an open BLOCKER/HIGH); four **final whole-branch reviews** (research-methodologist, journal-analyst, system-architect, quality-reviewer, run in parallel against the full branch diff since `main`) found a genuine BLOCKER (the experiment pipeline was never wired end-to-end, independently confirmed by three different reviewers) plus several real HIGH/MEDIUM findings across all four review dimensions, resolved in a three-part fix wave (see the top-level Milestone 7 summary above for the full breakdown), each wave independently re-reviewed clean. See `.superpowers/sdd/2026-09-23-milestone-7-ai-research/progress.md` for the full per-task and final-review ledger.

## Remaining

(none currently outstanding for Milestones 1, 2, 3, 5, 6, or 7; see `docs/roadmap.md` for what's next, the "Structure/Regime/Critic/Event agents" milestone, explicitly NOT started, per Milestone 7's own closing instruction not to begin live agent work automatically. There is no separate "Milestone 4," see `docs/roadmap.md`'s note. Milestone 7 is complete on branch `milestone-7-work` and has not yet been merged to `main` as of this writing.)

## Known limitations (Milestone 7)

See `docs/ai-research.md`'s own "Known limitations" section for the full detail behind each of these; summarized here for the living-tracker format this file uses elsewhere:

- Status advancement through the dataset-role pipeline (including `markPaperCandidate`'s own preconditions) is completion-based, not outcome-based: a `COMPLETED` experiment means the backtest ran, not that the strategy passed any performance bar. Acceptable for this foundation milestone because the final `PAPER_CANDIDATE` transition is explicitly human-gated; a future milestone should add recorded acceptance criteria and the robustness checks `docs/research-methodology.md` already requires.
- The sample-size guardrail's population is system-wide (every closed journal trade), not scoped to an experiment's own backtest trades: `ResearchHypothesis`/`ResearchExperiment` carry no `strategyId`/`instrumentId` column to scope by.
- `AgentExecution.costUsd` is `DECIMAL(10,4)`, rounding small per-call costs (does not affect token-based budget enforcement or any trading math; a follow-up migration to widen the scale is recommended).
- Stage-order and window-isolation preconditions are application-level read-then-decide checks, re-checked authoritatively inside the creation transaction but not fully race-proof for two genuinely simultaneous requests on non-final-test stages (only the final-test-reuse rule is a hard database constraint), proven safe for the one case tested (final-test reuse) with a live 5-round concurrent-creation test; accepted as proportionate for a single-operator platform, matching this project's established risk tier for similar low-frequency races.
- A completion-hook failure with the research queue's `attempts: 1` policy has no automatic recovery path: a `ResearchExperiment` can be left `RUNNING` indefinitely, permanently consuming a hypothesis's `FINAL_TEST` slot. A future fix would add a reconciliation sweep, mirroring `WebhookReconciliationProcessor`'s Milestone 3 precedent.
- `apps/dashboard`'s `/research/hypotheses` pages are read-only: no UI exists yet to trigger `POST /research/hypotheses/generate`, create an experiment, or call `mark-paper-candidate`; every write action requires a direct API call today.
- Consistent with every other internal API route in this codebase, the research endpoints have no authentication or authorization layer (a personal, single-user tool).

## Known limitations (Milestone 6)

**Resolved as a post-merge technical-debt pass (branch `tech-debt-fixes`, merged to `main` at `85ab4e2`), before Milestone 7 began:**

- ~~`execute()`/`skip()`'s guard doesn't close a true concurrent race~~ — **fixed**. A partial unique index (`JournalTrade_setupId_active_decision_key`, migration `20260923144150_add_journal_trade_active_decision_unique_index`: `CREATE UNIQUE INDEX ... ON "JournalTrade" ("setupId") WHERE "status" IN ('OPEN','PLANNED','SKIPPED')`) now backs the existing app-level `findJournalTradeBySetupId` fast-path check. `execute()`/`skip()` catch the resulting P2002 (verified against a live constraint violation, matched by `meta.target`, not a blind catch) and translate it into the identical `ConflictException` the fast-path already throws, so a caller can never distinguish a double-click rejection from a genuine race rejection. Proven with a real live-Postgres test: 10 genuinely concurrent `execute()` calls against one Setup converge on exactly one `OPEN` `JournalTrade`; a simultaneous `execute()`/`skip()` pair converges on exactly one row. `DomainErrorFilter` also catches this error as a defensive fallback for any other caller of the repository functions.
- ~~`closeJournalTrade`'s check-then-act race~~ — **fixed**. The final write is now an atomic conditional `updateMany({where:{id,status:"OPEN"}})`, mirroring `markScreenshotReady`/`markNotificationSent`; a miss re-fetches the row and throws an accurate `JournalTradeStateError` instead of silently letting a second concurrent close overwrite the first's data. Proven with a live-Postgres concurrency test.
- ~~`trade-screenshots.ts`'s unguarded `FAILED`-retry-reset race~~ — **fixed**. Applied the identical `updateMany`-with-status-guard fix already proven for `requestOrRetryNotification` during Milestone 6. Proven with a live-Postgres concurrency test.
- ~~`AnalyticsStatGrid.tsx`'s MFE/MAE currency mislabeling~~ — **fixed**. Verified both `BacktestTrade` and `JournalTrade` sources are unit-consistent (points, not dollars, confirmed all the way through persistence), then switched to `formatDecimal`/points labeling, matching the trade-detail page's own fix.

**Still open:**

- `POST /setups/:id/execute` and `/skip`, like every other internal API route in this codebase, have no authentication or authorization layer. Consistent with this project's existing documented posture (a personal, single-user tool).
- `AnalyticsStatGrid.tsx`'s "Avg Slippage" tile uses `formatCurrency` on a free-form, user-entered value (`JournalTrade.actualSlippage`/`estimatedSlippage`) with no documented unit convention anywhere (unlike MFE/MAE, which had a provable, objective computation trail). Not confirmed as a bug — genuinely ambiguous what unit a human is expected to enter — flagged for a documentation/UX follow-up (clarify the label), not a calculation fix.

## Known limitations (Milestone 5)

- `/internal/render/setup/[setupId]` and `/internal/render/trade/[tradeId]` have no runtime authorization beyond "not linked from navigation" — consistent with the rest of the dashboard, which has no auth model anywhere today. Before any deployment reachable beyond localhost, these routes need network-level or auth protection (a shared-secret header or IP allowlist in Next.js middleware, verified by the worker's `page.goto` call).
- A screenshot row stuck at `GENERATING` (e.g. the worker process crashes mid-render) has no automatic recovery path — only `FAILED` rows are recoverable via retry. Out of scope for this milestone; would need either a stale-`GENERATING` sweep or a render-attempt timeout.
- `apps/dashboard/src/lib/api.ts` hand-duplicates `ScreenshotType`/`ScreenshotStatus`/`TradeScreenshot` instead of importing them from `@trading-copilot/shared-types`, because that package's barrel transitively pulls in `node:crypto` (via `tradingview.ts`), which is unsafe in a browser bundle. Pre-existing debt from before this milestone (webhook/realtime types already show the same pattern); a real fix means splitting `shared-types`' export surface into a browser-safe and a server-only entry point.
- The `20260923044642_add_screenshot_generation` migration adds `updatedAt TIMESTAMP(3) NOT NULL` with no default — only safe because `TradeScreenshot` was empty at migration time; would fail against a populated table. No impact today (pre-launch project), but worth remembering for migration hygiene going forward.

## Known decisions

- `Candle.timeframe` and `Backtest.timeframe` are plain `String` columns validated against `TIMEFRAMES`, not Prisma enums — Prisma enum values must be valid identifiers and values like `"1m"` are not.
- Same-candle stop-and-target ambiguity resolves to "stop hit first" (see `docs/backtesting-assumptions.md`).
- Entry timing is next-bar open after a signal candle's close, to prevent look-ahead bias.
- Test runner is Vitest across every package and app for a coherent toolchain, including the NestJS apps (`@nestjs/testing` has no Jest-specific dependency).
- BullMQ queue name `"backtest-run"`, job name `"run"`, payload `{ backtestId }` only — the worker re-fetches everything else from Postgres so the job payload never goes stale relative to the DB.
- Same-candle stop-and-target exits apply the same adverse slippage as a plain STOP exit, for consistency (fixed after an initial pass omitted it).
- `initialBalance`, `tickSize`, `tickValue`, `pointValue` reject a zero value at the API/schema boundary (synchronous 400) rather than only failing later inside `packages/risk-engine`; `commissionPerContract` and `riskPercentage` still legitimately allow zero. `POST /backtests` also rejects `startDate >= endDate`.
- The dashboard's Research pages show a persistent banner noting the synthetic data and untuned strategy parameters, so that context isn't only discoverable in `docs/`.
- `JournalTrade` is intentionally distinct from `BacktestTrade` — never duplicated into it. `packages/database`'s `getNormalizedTrades()` merges both in memory at query time for `packages/analytics`.
- `JournalEvent.eventType`/`entityType` are real Prisma enums (stronger DB-level validation), not plain strings, unlike `Candle.timeframe` — their value sets don't start with digits, so the Prisma-enum-identifier problem that forced `timeframe` to be a string doesn't apply here.
- Every event in a `Setup`'s lifecycle (including its `JournalTrade`'s) shares `correlationId = setup.id`, making `GET /setups/:id/timeline` a single indexed query rather than a cross-table join.
- A `plannedRisk` of exactly `"0"` on a `JournalTrade` is treated identically to `null` (no risk baseline) at close time — `rMultiple` is `null`, never a thrown error or a fabricated `0` (a caller can't distinguish "unset" from "typed zero" through the API, so both get the same safe handling).
- `NestJS`'s `@UsePipes` at the method level applies to *every* parameter, not just `@Body()` — any handler combining `@Param()` with a Zod-validated body must apply `ZodValidationPipe` at the parameter (`@Body(new ZodValidationPipe(schema))`), not the method, or the path param gets incorrectly validated against the body schema too.
- `getNormalizedTrades()` sorts its merged output by `entryTimestamp` (both source queries are also individually ordered, but concatenating two sorted lists doesn't itself produce a globally sorted one) — `packages/analytics` never re-sorts, so this is the only place ordering can be guaranteed.
- `createPostTradeAnalysis` verifies the referenced `BacktestTrade`/`JournalTrade` exists before inserting (404 via `NotFoundError` otherwise) — the only integrity check available for a polymorphic reference that can't have a real database foreign key.
- `createStrategyVersion` emits `STRATEGY_VERSION_PROPOSED` in the same transaction as the create — no `StrategyVersion` row exists without a corresponding journal entry.
- `/analytics` and its strategy-version detail page carry an `AnalyticsDisclaimer` banner (mirroring `/research`'s `ResearchDisclaimer`) since a single row can blend simulated backtest trades and real/paper journal trades — check the trades table's `Source` column before treating a number as evidence about live performance.
- `InboundWebhookEvent.fingerprint` excludes `firedAt` (wall-clock delivery time) and the OHLCV fields from its canonicalized input, keyed only on `provider/strategyKey/strategyVersion/exchange/symbol/timeframe/signal/direction/barTime`. A genuine retry of the same trigger can have a different `firedAt`, and including OHLCV would make the fingerprint fragile to harmless formatting differences between deliveries of the same bar.
- Instrument and strategy resolution for a TradingView webhook never guess: an unmapped `exchange`/`symbol` is `UNKNOWN_INSTRUMENT`, an unmatched `strategyKey`/`strategyVersion` pair is `UNKNOWN_STRATEGY_VERSION` (never "fall back to latest version"), both rejected with the `InboundWebhookEvent` row kept for audit, never silently dropped.
- `Setup.sourceWebhookEventId` (nullable, `@unique`) is the idempotency guard against a BullMQ retry creating a second `Setup` for one webhook delivery: the same "database constraint, not check-then-insert" principle as `fingerprint`, added after a review found the original retry-safety logic checked only `InboundWebhookEvent.processingStatus`, which is insufficient on its own (a crash between `createSetup` and the event being marked `PROCESSED` leaves it at `PROCESSING`/`FAILED`, neither of which blocks reprocessing).
- The setup-expiration BullMQ job is scheduled *before* the `InboundWebhookEvent` is marked `PROCESSED`, not after. Marking `PROCESSED` blocks any future retry, so scheduling first means a crash between the two steps just means a retry (or the sourceWebhookEventId recovery path) schedules the expiration job again, which is harmless, rather than the job never being scheduled at all.
- A synchronously-`400`-rejected webhook delivery (envelope-invalid, or `schemaVersion: 1` but the full v1 shape is invalid) is still durably persisted as `REJECTED`/`MALFORMED_PAYLOAD`, using a fallback fingerprint (a hash of the whole raw body, since the real field-based fingerprint needs specific fields to exist and parse). The HTTP response stays an unchanged, fast, synchronous 400.
- `WebhookProcessingStatus.DUPLICATE` is a reserved enum value, never actually set by any code path: a repeat delivery never gets its own row, and the original row's status is deliberately never overwritten (doing so would incorrectly imply an already-successful delivery was invalidated). "This was redelivered" is conveyed instead via the `wasDuplicate` response flag and a `WEBHOOK_DUPLICATE_DETECTED` journal event.
- `apps/api`'s and `apps/worker`'s `dev` scripts use `tsc --watch` + `node --watch dist/main.js`, not `tsx watch`. `tsx` transpiles via esbuild, which does not emit the TypeScript decorator metadata NestJS's dependency injection depends on for implicit constructor-based injection, so any such provider silently read as `undefined` at request time under the old script.
- `NotificationDelivery.failureCode`/`failureMessage` are always drawn from a small, fixed, known set (`classify()` in `telegram-notification.provider.ts`, plus the processor's own `SETUP_NOT_FOUND`/`SETUP_CONTEXT_NOT_FOUND`/`UNKNOWN_ERROR`), never the raw provider error text - a Telegram error response could in principle echo request details, including the bot token, back in its description.
- `isTelegramConfigured` (the three-env-var Telegram-configured check) has exactly one implementation, in `packages/shared-types/src/notifications.ts`; `apps/worker`'s `notification-provider.factory.ts` re-exports it rather than duplicating it, after an integration pass found Tasks 5 and 10 had independently landed byte-identical duplicate implementations.
- `JournalTrade.mfe`/`mae`/`outcome` cannot be set through the API at all as of Milestone 6: `closeJournalTradeSchema` has no such fields, and both are computed inside `closeJournalTrade` itself. A `JournalTrade` with no `Setup` lineage (`setupId: null`) gets `mfe: null`/`mae: null` (no timeframe to know which candle series to query) - this is expected, not a bug.
- `POST /setups/:id/execute`'s `plannedStop` fallback to `plannedEntry` (when the `Setup`'s own `plannedStop` is `null`) is genuinely load-bearing, not dead code: `transitionSetupStatus`'s transition matrix has no precondition requiring `plannedStop`/`plannedTarget1` to be non-null before a `Setup` can reach `READY`, so a `TRADINGVIEW`-sourced setup can legitimately be `READY` with only a candidate entry known. Confirmed live against a real `READY` setup with `plannedStop: null` during Task 9's review.

## Known blockers

(none)

## Local environment notes

- On this machine, port 6379 is already used by an unrelated project's Redis container, so local `.env` maps `REDIS_PORT=6380` / `REDIS_URL=redis://localhost:6380`. `.env.example` still documents the conventional default (6379) since a clean machine won't have this conflict.
